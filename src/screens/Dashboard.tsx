import React, { FC } from "react";
import { Text } from "../components/Text";
import { Screen } from "../components/Screen";

export const Dashboard: FC = () => {
  return (
    <Screen>
      <Text type="body">Dashboard screen</Text>
    </Screen>
  );
};
