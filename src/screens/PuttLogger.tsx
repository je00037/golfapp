import React, { FC } from "react";
import { Text } from "../components/Text";
import { Screen } from "../components/Screen";

export const PuttLogger: FC = () => {
  return (
    <Screen>
      <Text type="body">Putt Logger screen</Text>
    </Screen>
  );
};
